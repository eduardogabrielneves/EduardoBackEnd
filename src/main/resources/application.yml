spring:
  application:
    name: alunos
  datasource:
    url: jdbc:mysql://localhost:3306/alunoapi
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
  server:
    servlet:
      encoding:
      charset: utf-8
      enabled: true
      force: true